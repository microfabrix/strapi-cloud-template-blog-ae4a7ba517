'use strict';

/**
 * foreman-job-installation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::foreman-job-installation.foreman-job-installation');
