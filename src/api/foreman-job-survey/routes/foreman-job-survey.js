'use strict';

/**
 * foreman-job-survey router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::foreman-job-survey.foreman-job-survey');
