'use strict';

/**
 * foreman-job-survey service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::foreman-job-survey.foreman-job-survey');
